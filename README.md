Digital Muse Studio — Text Creation Studio

Vercel deployment package. The API routes in /api are automatically detected by Vercel; no custom function runtime configuration is required.

Environment variables:
SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY
ADMIN_ACCESS_CODE
ACCESS_ISSUE_WEBHOOK_SECRET
